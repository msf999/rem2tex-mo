node_modules
.DS_Store
dist
dist-ssr
*.local
*.zip
TODO.txt

# Cross-machine dev log / working notes — kept local, not committed (mo, 2026-09-02).
CLAUDE.md

# Live-test rig: drives the real plugin in the real app through Chrome DevTools. Local only,
# like CLAUDE.md — it carries a source patch (live-test/hook.js) that must never ship.
live-test/
